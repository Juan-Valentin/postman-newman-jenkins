{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/Jsonplaceholder.com.postman_collection.json --reporter junit --reporter-junit-export newman.xml"
  },
  "author": "Valentin Juan",
  "dependencies": {
    "newman": "^5.2.0"
  }
}
